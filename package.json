{
  "name": "cmdeck-desktop",
  "version": "1.0.0",
  "description": "Command Deck Desktop — Live wallpaper and screensaver for cmdeck.io",
  "main": "dist/main/index.js",
  "author": "Marcus Dycus",
  "license": "MIT",
  "scripts": {
    "dev": "tsc && unset ELECTRON_RUN_AS_NODE && electron .",
    "build": "tsc",
    "dist": "tsc && electron-builder",
    "dist:mac": "tsc && electron-builder --mac",
    "dist:win": "tsc && electron-builder --win"
  },
  "dependencies": {
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^41.1.1",
    "electron-builder": "^25.1.0",
    "typescript": "^5.6.0"
  },
  "optionalDependencies": {
    "electron-as-wallpaper": "^3.1.0"
  }
}